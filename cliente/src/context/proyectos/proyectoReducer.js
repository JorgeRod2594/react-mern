export default (state, action) => {
    //El recuder funciona igual que en redux
    //Evaluamos las acciones medieante casos 
    switch(action.type){
        default: //siempre se retorna un default state
            return state;

    }

}